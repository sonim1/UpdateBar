# Verification tasks

- Add grouping, selection reconciliation, action-guard and mutation regressions.
- Replace the Items table with native cards/status sections and share menu state/actions.
- Verify focused tests and the complete Scripts/quality-gate.sh.
- Exercise isolated native fixture updates, approval, stop/retry, filtering, disclosure and window/theme behavior.
- Bind independent review and runtime evidence to the final commit before release.
- Publish through the existing automatic version/tag/signing/notarization/appcast workflow and verify artifacts.

Live execution status and safe test receipts are maintained in the task evidence ledger; this file specifies the required release checks.
